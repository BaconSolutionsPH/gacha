// UI component prop types and utility types

import type { ClassValue } from 'clsx'

export type { ClassValue }

// Re-export common React types that might be used across components
export type { ReactNode, ComponentProps } from 'react'
